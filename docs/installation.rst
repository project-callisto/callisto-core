============
Installation
============

At the command line::

    $ easy_install callisto-core

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv callisto-core
    $ pip install callisto-core
