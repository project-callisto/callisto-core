============
Installation
============

At the command line::

    $ easy_install django-wizard-builder

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-wizard-builder
    $ pip install django-wizard-builder
